<Window x:Class="SnippetIO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snippet Manager" Height="450" Width="800"
        Background="#222" Foreground="White" FontFamily="Segoe UI">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <!-- List Area -->
            <ColumnDefinition Width="3*" />
            <!-- Input Area -->
        </Grid.ColumnDefinitions>

        <!-- Left: Snippet List -->
        <ListBox x:Name="SnippetList"
                 Grid.Column="0"
                 Background="#333"
                 Foreground="White"
                 BorderThickness="0"
                 Padding="10"
                 FontSize="14"/>

        <!-- Right: Input Section -->
        <StackPanel Grid.Column="1" Margin="20">
            <!-- Shortcut Input -->
            <TextBlock Text="Shortcut:" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="NewShortcutBox"
                     Height="35"
                     Background="#444"
                     Foreground="White"
                     BorderThickness="0"
                     Padding="5"
                     FontSize="14"
                     IsReadOnly="True"/>

            <!-- Snippet Content -->
            <TextBlock Text="Snippet Content:" FontSize="16" Margin="0,15,0,5"/>
            <TextBox x:Name="SnippetContentBox"
                     Height="200"
                     Background="#444"
                     Foreground="White"
                     BorderThickness="0"
                     Padding="5"
                     FontSize="14"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
